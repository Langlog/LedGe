# Ledge 0.1.3

本版本修复窗口收起与 Finder 文件拖移问题。

Ledge 是一个藏在 Mac 屏幕底部的轻量临时工作台。指针靠近底部时显示小标签，点击后直接进入便签；拖动 Finder 文件时则自动打开文件中转站。

主要功能：

- 单一纯文本便签，自动本地保存。
- 多文件和文件夹真实移动中转，不创建快捷链接。
- 拖入时源位置消失，拖出时移动到目标位置。
- `Esc` 快速收回主窗口。
- 点击展开窗口之外的位置时，窗口会播放动画并自动收回。
- 使用系统文件承诺机制修复 Finder 拖出时可能出现的 `-43` 或 `-8058` 错误；只有文件完整落盘后才清理中转源文件。
- 便签支持 `Ctrl+A`、`⌘A`、`⌘C` 和 `⌘V`。
- 菜单栏设置、多显示器、登录启动和唤醒灵敏度；成功拖出后自动移除。
- 原生深色模式、无 Dock 图标、无网络请求和遥测。

系统要求：macOS 13 或更高版本。

已知分发限制：当前本机没有 Developer ID Application 证书，因此本地生成的 DMG 是 ad-hoc 签名测试包。面向公众发布前需要使用 Developer ID 重新签名并完成 Apple 公证。
