# 更新记录

本文档遵循 Keep a Changelog 的结构，版本号遵循语义化版本。

## [0.1.3] - 2026-08-22

### 修复

- 使用 macOS `NSFilePromiseProvider` 重写 Ledge 到 Finder 的拖出流程，消除 Finder 尚未完成读取时源文件被提前删除导致的 `-43` 错误。
- 文件与文件夹先写入同目录临时项，完整写入后原子交付；失败或取消时保留中转源文件。
- “拖出后移除”现在只在 Finder 文件承诺成功完成后执行。

## [0.1.2] - 2026-08-22

### 修复

- 修正 Finder 拖入协议：Ledge 接收阶段先复制到中转区，再由 Finder 完成源文件移除，避免 `-43` 和 `-8058` 错误。
- 中转保存失败时只清理内部副本，确保用户原文件不被误删。

## [0.1.1] - 2026-08-22

### 修复

- 展开窗口后，点击窗口外部会用收起动画自动隐藏。
- 修复 Finder 拖入和拖出文件时可能出现的 `-8058` 移动冲突。

## [0.1.0] - 2026-08-02

### 新增

- 底部贴边小标签及确认展开动画。
- 默认打开纯文本便签，内容自动本地保存。
- Finder 多文件和文件夹中转，采用真实移动语义。
- 菜单栏 Settings、多显示器、灵敏度、登录启动和拖出后移除设置。
- 深色模式适配。
- `Ctrl+A`、`⌘A`、`⌘C`、`⌘V` 编辑快捷键。
- `Esc` 快速收回便签和文件移动主窗口。
